import type { PlatformRuntimeContext } from '../protocol/commands';
export type WorkMode = 'chat' | 'immediate_task' | 'scheduled_task' | 'scheduled_multi_task';
export type PresentationContract = { uiFormat: 'chat_message' | 'table' | 'report' | 'artifact'; ttsEnabled: boolean; ttsMode: 'summary' | 'full'; ttsMaxChars: number; language: string };
export type DeliverableContract = { id: string; title: string; type: 'chat_reply' | 'report_file' | 'artifact_file' | 'workspace_change' | 'code_change'; description: string; required: boolean; path?: string; format?: string };
export type HitlRiskTier = 'low' | 'medium' | 'high';
export type HitlExecutionPolicy = 'auto' | 'review_required' | 'hard_block';
export type CheckpointContract = { id: string; title: string; kind: 'review' | 'manual_action' | 'pre_delivery'; reason: string; userMessage: string; riskTier: HitlRiskTier; executionPolicy: HitlExecutionPolicy; requiresUserConfirmation: boolean; blocking: boolean };
export type UserActionRequest = { id: string; title: string; kind: 'clarify_input' | 'confirm_plan' | 'manual_step' | 'external_auth'; description: string; riskTier: HitlRiskTier; executionPolicy: HitlExecutionPolicy; blocking: boolean; questions: string[]; instructions: string[]; fulfillsCheckpointId?: string };
export type HitlPolicy = { riskTier: HitlRiskTier; requiresPlanConfirmation: boolean; reasons: string[] };
export type TaskHardness = 'trivial' | 'bounded' | 'multi_step' | 'externally_blocked' | 'high_risk';
export type RequiredCapability = 'browser_interaction' | 'external_auth' | 'workspace_write' | 'host_access' | 'human_review';
export type BlockingRisk = 'none' | 'missing_info' | 'auth' | 'permission' | 'manual_step' | 'policy_review';
export type InteractionMode = 'passive_status' | 'input_first' | 'action_first' | 'review_first';
export type ExecutionShape = 'single_step' | 'staged' | 'exploratory' | 'deterministic_workflow';
export type SocialPublishPlatform = 'x' | 'xiaohongshu' | 'wechat_official' | 'reddit' | 'facebook' | 'instagram' | 'linkedin' | 'generic_social';
export type PublishExecutionMode = 'direct_publish' | 'preview_then_publish' | 'draft_only';
export type PublishIntent = { action: 'publish_social_post'; platform: SocialPublishPlatform; executionMode: PublishExecutionMode; requiresSideEffect: boolean };
export type ExecutionProfile = { primaryHardness: TaskHardness; requiredCapabilities: RequiredCapability[]; blockingRisk: BlockingRisk; interactionMode: InteractionMode; executionShape: ExecutionShape; reasons: string[] };
export type TaskExecutionRequirement = { id: string; kind: 'tool_evidence'; capability: 'browser_interaction' | 'shell_execution'; required: boolean; reason: string };
export type TaskDefinition = { id: string; title: string; objective: string; constraints: string[]; acceptanceCriteria: string[]; dependencies: string[]; preferredSkills: string[]; preferredTools: string[]; preferredWorkflow?: string; resolvedTargets?: string[]; sourceUrls?: string[]; executionRequirements?: TaskExecutionRequirement[] };
export type ClarificationDecision = { required: boolean; reason?: string; questions: string[]; missingFields: string[]; canDefault: boolean; assumptions: string[] };
export type WorkRequestFollowUpContext = { baseObjective?: string; latestAssistantMessage?: string; recentMessages?: Array<{ role: 'user' | 'assistant'; content: string }> };
export type IntentRouting = { intent: 'chat' | 'immediate_task' | 'scheduled_task'; confidence: number; reasonCodes: string[]; needsDisambiguation: boolean; forcedByUserSelection?: boolean };
export type ResearchSource = 'conversation' | 'workspace' | 'memory' | 'connected_app' | 'web' | 'template';
export type ResearchKind = 'domain_research' | 'context_research' | 'feasibility_research';
export type ResearchQuery = { id: string; kind: ResearchKind; source: ResearchSource; objective: string; directUrls?: string[]; required: boolean; status: 'pending' | 'running' | 'completed' | 'failed' | 'skipped' };
export type ResearchEvidence = { id: string; kind: ResearchKind; source: ResearchSource; summary: string; confidence: number; uri?: string; artifactPath?: string; collectedAt: string };
export type UncertaintyItem = { id: string; topic: string; status: 'confirmed' | 'inferred' | 'blocking_unknown' | 'defaultable'; statement: string; whyItMatters: string; question?: string; defaultValue?: string; supportingEvidenceIds: string[] };
export type StrategyOption = { id: string; title: string; description: string; pros: string[]; cons: string[]; feasibility: 'high' | 'medium' | 'low'; supportingEvidenceIds: string[]; selected: boolean; rejectionReason?: string };
export type ReplanPolicy = { allowReturnToResearch: boolean; triggers: Array<'new_scope_signal' | 'missing_resource' | 'permission_block' | 'contradictory_evidence' | 'execution_infeasible'> };
export type NormalizedWorkRequest = { schemaVersion: 1; mode: WorkMode; intentRouting: IntentRouting; taskDraftRequired?: boolean; sourceText: string; workspacePath: string; environmentContext?: PlatformRuntimeContext; schedule?: { executeAt?: string; timezone: string; recurrence?: null | { kind: 'rrule'; value: string }; stages?: Array<{ taskId: string; executeAt: string; delayMsFromPrevious?: number; originalTimeExpression?: string }> }; tasks: TaskDefinition[]; clarification: ClarificationDecision; presentation: PresentationContract; deliverables?: DeliverableContract[]; checkpoints?: CheckpointContract[]; userActionsRequired?: UserActionRequest[]; executionProfile?: ExecutionProfile; publishIntent?: PublishIntent; hitlPolicy?: HitlPolicy; missingInfo?: Array<{ field: string; reason: string; blocking: boolean; question?: string }>; researchQueries?: ResearchQuery[]; researchEvidence?: ResearchEvidence[]; uncertaintyRegistry?: UncertaintyItem[]; strategyOptions?: StrategyOption[]; selectedStrategyId?: string; knownRisks?: string[]; replanPolicy?: ReplanPolicy; createdAt: string };
export type FrozenWorkRequest = NormalizedWorkRequest & { id: string; frozenAt: string; frozenResearchSummary?: { evidenceCount: number; sourcesChecked: ResearchSource[]; blockingUnknownCount: number; selectedStrategyTitle?: string } };
export type ExecutionPlanStepKind = 'analysis' | 'clarification' | 'goal_framing' | 'research' | 'uncertainty_resolution' | 'contract_freeze' | 'execution' | 'reduction' | 'presentation';
export type ExecutionPlan = { workRequestId: string; runMode: 'single' | 'dag'; steps: Array<{ stepId: string; taskId?: string; kind: ExecutionPlanStepKind; title: string; description: string; status: 'pending' | 'running' | 'completed' | 'failed' | 'blocked'; dependencies: string[] }> };
